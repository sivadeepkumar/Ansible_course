- name : Install and run Nginx
  hosts: web
  become : yes
  tasks: 
  - name: Install nginx into linux server
    ansible.builtin.package:
      name: nginx
      state: present
  
  - name: Run nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes